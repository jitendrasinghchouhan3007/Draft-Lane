{
  "name": "trying-something",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "demo:prepare": "node db/setup-demo.mjs",
    "demo:seed": "npm run seed --prefix backend",
    "demo:bootstrap": "npm run install:all && npm run demo:prepare && npm run demo:seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}